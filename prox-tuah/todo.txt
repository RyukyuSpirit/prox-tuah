- complete all pve api contexts:
    - access
    - cluster
    - nodes (WIP)
    - pools
    - storage
    - version

- allow standard args or flags (non-kwarg)

- mark 'required' params in help

- add quote wrapping for string kwarg values without quotes (current fails unless quotes are explicitly around the values)

- special 'doc' hotkey/param for api contexts to open related web-page from pve API site

- function to display api commands in HTTP/CLI form rather than execute them (ex. dry-run)

- add output sorting

- 'top' global cmd to return to top or to execute from any sub-context

- Add add'l connection methods:
    - VNC
    - RDP
    - SSH
    - Terminal

- Context entry validation (handler func that returns true if sub-context can be entered)